import * as path from 'path';

export const ROOT_PATH = path.join(__dirname, '..');
export const SRC_PATH = path.join(ROOT_PATH, 'src');
export const LOG_PATH = path.join(ROOT_PATH, 'log');
export const FILES_PATH = path.join(ROOT_PATH, 'files');
export const CONFIG_PATH = path.join(ROOT_PATH, 'config');
